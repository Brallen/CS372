To run the program first run build to create the client program. Its an executable called chatclient

Next start up the server by running "python clientserve.py [portNumber]"
	- if there is no port number specified it will default to a specified port

Then start up the client by running "chatclient [hostname] [portnumber]"
